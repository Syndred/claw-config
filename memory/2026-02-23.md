# 2026-02-23 周一 08:24

## 定时任务修复全过程

### 问题排查
1. **表象**: cron list 空，手动添加任务返回 `No response generated`
2. **定位**: `gateway-client` 设备在 `paired.json` 仅有 `operator.read`，缺少 `admin` scope
3. **深层**: 内嵌 agent 访问 gateway 需要 `pairing` scope，cron wake 触发失败

### 修复步骤
1. 编辑 `/root/.openclaw/devices/paired.json`，给 `gateway-client` 加全 scopes
2. 多次重启 gateway，scope 生效
3. 重建三个日常定时任务
4. 将 `isolated` agentTurn 任务改为 `main` session systemEvent 模式（避免子 agent delivery 依赖）
5. 关闭 gateway auth (`mode: "none"`) 解决 `pairing required` 硬性要求

### 任务配置现状
```
🌅 早安日报      - 08:00 (main session) - systemEvent 触发，手动执行早安日报
🙋 询问今日工作  - 22:30 (main) - systemEvent 提醒询问
🌙 统一汇报     - 23:30 (main) - systemEvent 提醒执行收尾流程
```

### 技术教训
- **权限设计**: gateway-client（内嵌 agent 的 WS 连接）必须拥有 `operator.admin/pairing` 才能调用 gateway 工具
- **session 模式**: isolated agentTurn 依赖 delivery 推到 channel，易受配对问题影响；main systemEvent 更可靠
- **定时测试**: 用 `cron add --at <UTC时间>` 快速验证，而不是依赖 future 的整点

### 待观察
- 今晚 22:30 能否正确收到 `🙋 询问今日工作` 提醒
- 如果收不到，检查系统日志中 `gateway/heartbeat` 是否正常触发
- 最坏方案：改用纯 CLI cron 调用 `openclaw send` 绕过 gateway

## 用户备忘
- 主人早上查看了官网文档，一起解决了 cron 触发问题
- 修复后系统状态正常，等待今晚验证