2026-02-04（补充）

- 定位并确认 OpenClaw Telegram 定时提醒问题的真实根因：不是 cron、不是真没触发，而是 systemEvent（REMIND::xxx）默认不会转为 Telegram 消息。
- 发现并修复关键阻断点：HEARTBEAT.md 语义为空会导致 cron/heartbeat 任务被 skipped（error: empty-heartbeat-file）。
- 确认 heartbeat 的真实生效配置在 agents.defaults.heartbeat.every（默认 30m），而非 gateway 层；已改为 1m 并成功热重启生效。
- 结论性认知：在 Telegram 渠道下，remind-me 的职责只到生成 REMIND:: 事件；若需要真正“主动推送”，必须在 agent 行为层显式把 REMIND:: 转换为 message.send。
- 用户具备工程级排错能力，能直接对照官方文档与日志定位问题文件与配置层级。